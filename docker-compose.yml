services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: oxid
      POSTGRES_PASSWORD: oxid
      POSTGRES_DB: oxid_fenix
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
